export type Plant = {
  id: string;
  name: string;
  species: string;
  link: string;
};

export type PlantDetail = {
  species: string;
  link: string;
  introduction: string;
  description: string;
};
